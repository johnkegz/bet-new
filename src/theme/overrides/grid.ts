// import { palette } from '../palette';

const MuiGridTheme = {
    props: {},
    overrides: {
        root: {
            "& div[role='tabpanel']": {
                width: '100%'
            }
        },
    }
}

export default MuiGridTheme;