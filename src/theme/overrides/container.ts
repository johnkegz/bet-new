//import { palette } from '../palette';

const MuiContainerTheme = {
    props: {
        maxWidth: false,
        disableGutters: true,
    },
    overrides: {
        root: {
        },
    }
}

export default MuiContainerTheme;