//import { palette } from '../palette';

const MuiFilledInputTheme = {
    props: {},
    overrides: {
        root: {
        },
        filled: {
        },
        outlined: {
        }
    }
}

export default MuiFilledInputTheme;