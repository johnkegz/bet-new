//import { palette } from '../palette';

const MuiInputAdornmentTheme = {
    props: {},
    overrides: {
        root: {
        },
        filled: {
        },
        outlined: {
        }
    }
}

export default MuiInputAdornmentTheme;