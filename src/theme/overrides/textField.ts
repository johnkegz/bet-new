//import { palette } from '../palette';

const MuiTextFieldTheme = {
    props: {},
    overrides: {
        root: {
        },
        filled: {
        },
        outlined: {
        }
    }
}

export default MuiTextFieldTheme;