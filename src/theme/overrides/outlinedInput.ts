//import { palette } from '../palette';

const MuiOutlineInputTheme = {
    props: {},
    overrides: {
        root: {
        },
        filled: {
        },
        outlined: {
        }
    }
}

export default MuiOutlineInputTheme;